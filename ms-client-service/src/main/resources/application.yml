server:
  port: 8081

spring:
  profiles:
    active: dev
  application:
    name: ms-client_service
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true