spring:
  application:
    name: ms-client-service
  datasource:
    url: jdbc:mysql://localhost:3306/sistema
    password: admin
    username: root
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
